import { Test, TestingModule } from '@nestjs/testing';
import { AmplifyConnectionProviders } from './amplify-connection.providers';

describe('Amplify', () => {
  // let provider: AmplifyConnectionProviders;

  // beforeEach(async () => {
  //   const module: TestingModule = await Test.createTestingModule({
  //     providers: [AmplifyConnectionProviders],
  //   }).compile();

  //   provider = module.get<AmplifyConnectionProviders>(AmplifyConnectionProviders);
  // });

  // it('should be defined', () => {
  //   expect(provider).toBeDefined();
  // });
});
